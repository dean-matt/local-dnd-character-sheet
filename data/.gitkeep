# `data/` holds the three SQLite databases, none of which are committed.
# This file only ensures the directory exists in a fresh clone, so that
# `pnpm db:studio` and `pnpm db:migrate` work before `pnpm content:build` has run.

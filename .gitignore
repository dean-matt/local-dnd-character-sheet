node_modules/
dist/
build/
*.tsbuildinfo

# Upstream 5etools data — fetched, never committed (see NOTICE)
vendor/5etools*

# Generated catalog — rebuilt by `pnpm content:build`
data/content.db*

# Your data — not committed, but back these up
data/characters.db
data/characters.db-*
data/homebrew.db
data/homebrew.db-*

# Backups taken before each migration — see docs/reviving.md to restore one
data/backups/

# Editors — share the project-level config, ignore anything personal
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/

# Agents — share the skills, ignore what belongs to one machine
.claude/settings.local.json
.claude/worktrees/

.env
.env.*
!.env.example
.DS_Store

# Playwright
/test-results/
/playwright-report/
/blob-report/
/.playwright/
